spring:
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: http://localhost:8080/realms/doctor-app-realm
  application:
    name: authorization-service
  cloud:
    config:
      uri: http://localhost:8888
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: pgtest
    password: pgtest
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update #for development is acceptable, for production better use flyway/liquidbase

management:
  endpoints:
    web:
      exposure:
        include: '*'

server:
  port: 8999

